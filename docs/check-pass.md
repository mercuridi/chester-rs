Yes. Reviewing `39ddc2e..e0905c9`, I found **six additional issues worth addressing**, plus two incomplete fixes. I excluded your deliberately outstanding items.

1. **High — Recording audio remains in memory until stop.** The encoder accumulates every compressed packet and writes them only after recording finishes. Memory grows with duration and participants; a crash loses the entire audio payload. **Confirmed with a probe:** after consuming 200 frames, the file contained only headers. Stream packets to disk, retaining just enough buffering to mark the final packet. [encoder.rs:184](/home/mercuridi/Documents/projects/chester-rs/src/chronicle/recording/encoder.rs:184)

2. **High — Concurrent downloads can delete successfully registered audio.** The per-ID lock covers downloading but ends before database insertion. Two requests can both pass the initial lookup; one inserts successfully, the other hits the duplicate constraint and deletes their shared MP3. **Confirmed with a probe:** one database row remained, but its audio was gone. Coordinate the complete registration operation and make duplicate completion idempotent. [download.rs:284](/home/mercuridi/Documents/projects/chester-rs/src/jester/track/download.rs:284)

3. **High — Repeating `/chronicle start` disables retrieval while reporting readiness.** An already-loaded LLM rejects another load; the error handler then unloads the existing embedder. The command sees the LLM still loaded and responds “Chronicle is already ready.” This predates the sequence but remains in the touched lifecycle code. Make startup idempotent and roll back only resources acquired by that attempt. [lifecycle.rs:11](/home/mercuridi/Documents/projects/chester-rs/src/chronicle/service/lifecycle.rs:11), [command response](/home/mercuridi/Documents/projects/chester-rs/src/discord/commands/chronicle.rs:57)

4. **Medium — Existing audio can become impossible to register after restart.** Metadata exists only in the downloader’s memory. If execution stops between publishing the MP3 and inserting its database row, subsequent attempts find the file, return no metadata, and fail registration indefinitely. **Confirmed the missing-metadata behavior with a fresh downloader instance.** Persist metadata alongside the artifact or fetch it when missing. [download.rs:142](/home/mercuridi/Documents/projects/chester-rs/src/jester/track/download.rs:142)

5. **Medium — Recovery cannot handle a session left `Recording` by a crash.** Recovery treats the persisted status as proof that a recorder is still active, so precisely those abandoned sessions are rejected after restart. Conversely, recovery accepts `Finalizing` without checking whether its encoder is still running. Check live ownership and serialize recovery with finalization. [recorder.rs:334](/home/mercuridi/Documents/projects/chester-rs/src/chronicle/recording/recorder.rs:334)

6. **Medium — Idle encoders busy-spin.** Each participant’s blocking worker repeatedly polls the ring buffer and calls `std::thread::yield_now()` when empty. Yielding does not suspend it until audio arrives, so silent recordings keep workers runnable and waste CPU. This is another pre-existing omission. Use an explicit wakeup mechanism that also handles stop notifications. [encoder.rs:129](/home/mercuridi/Documents/projects/chester-rs/src/chronicle/recording/encoder.rs:129)

Two completed items need follow-up:

- **D31 — Shutdown is not actually bounded.** The coordinator’s timeout cancels its async drain, but transcription workers are not tracked or drained. Normal Tokio runtime destruction then waits indefinitely for outstanding blocking workers. Track worker completion and apply an explicit process-level shutdown deadline. [shutdown.rs:68](/home/mercuridi/Documents/projects/chester-rs/src/shutdown.rs:68), [main.rs:335](/home/mercuridi/Documents/projects/chester-rs/src/main.rs:335)
- **D24 — Changed document bodies still accumulate before batching.** `index_discovered_documents` loads every changed document into `pending`; preparation batches begin only after discovery finishes. A full rebuild still retains the corpus bodies. Flush bounded batches while iterating. [service.rs:272](/home/mercuridi/Documents/projects/chester-rs/src/chronicle/indexer/service.rs:272)

**Validation:** all 329 existing tests and formatting passed. Three temporary probes confirmed the recording/download failures and were removed; the working tree is clean. Strict Clippy still fails on the unused planner wrapper and an evaluation `expect()`—within your outstanding D18. Other findings are code-traced; live Discord and GPU shutdown were not exercised.

I’d address **recording durability, download registration, and repeated Chronicle startup first**.